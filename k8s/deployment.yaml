apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-services
  labels:
    app: web-services
    dxp.io/managed: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web-services
  template:
    metadata:
      labels:
        app: web-services
        backstage.io/kubernetes-id: web-services
    spec:
      nodeSelector:
        role: workload
      tolerations:
      - key: role
        operator: Equal
        value: workload
        effect: NoSchedule
      containers:
      - name: web-services
        image: harbor.harbor.svc.cluster.local/dxp/web-services:8c2d4f7
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: 50m
            memory: 32Mi
          limits:
            cpu: 100m
            memory: 64Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: web-services
spec:
  selector:
    app: web-services
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-services
  labels:
    app: web-services
    dxp.io/managed: "true"
spec:
  ingressClassName: nginx
  rules:
  - host: web-services.68.221.5.247.nip.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: web-services
            port:
              number: 80
